# バックログ(マイルストーン積み残し・保留項目)

特定のマイルストーンに紐づけずに温めておく改善候補を管理する。
マイルストーンの計画時にここへ項目を移す/ここから拾うことで、
各 `docs/milestone-N-plan.md` のスコープを純粋に保つ。
着手するときは roadmap のマイルストーンに昇格させるか、単発の feature ブランチで対応し、
完了したら項目を消す(経緯はコミット履歴に残る)。

## 検索パネル UI の全面自作(出自: M13 #11・需要確認待ち)

- M13 では CodeMirror 標準検索パネルを「日本語化(phrases)+ Slate スタイル」で
  整えるに留めた。レイアウトごと変えたくなったら `search({ createPanel })` で
  パネル UI を自作し、検索・置換エンジンは引き続き @codemirror/search を流用する。

## 図の拡大表示(出自: M11 Mermaid・需要確認待ち)

- ガントチャート等の横長の図が本文カラム(46rem)に縮んで見にくい。
- 設定いじりの小手先対応ではなく、クリックで拡大(ライトボックス)や
  図だけ全幅表示のような機能として設計する。
- 当座はプレビューのフォントサイズ連動拡大(`--diagram-scale`)で代用できる。
- M12(Kroki 経由の PlantUML / Draw.io)で図の種類が増えると需要も増える見込み。
  対応するならリモート図(`<img>`)とローカル図(inline SVG)の両方に効く設計にする。

## HTML エクスポート時の `link:*.adoc` リンクを `.html` へ書き換え(出自: 2026-07-13 検討)

- `xref:foo.adoc[]` / `<<foo.adoc#sec,…>>` は Asciidoctor の `outfilesuffix` により
  既に `.html` へ自動変換される(実測確認済み)。問題は `link:` マクロだけで、
  対象を素通しする仕様のため `.adoc` のまま出力される。
- 対応方針: 「相対パスで `.adoc`(+`#fragment`)で終わる href を `.html` に
  書き換える」純関数を `core/` に置いてテストで固定し、
  `ui/html-export.ts` の `buildExportHtml` の後処理に組み込む。
  `http(s):` 等の外部 URL は書き換えない。小規模(30分〜1時間)。
- PDF(印刷)はライブプレビューを `window.print()` する方式のため本対応の範囲外。
  制限として [export-notes.md](export-notes.md) に記載済み。
  ヘルプページ(M19 で実装予定、[roadmap.md](roadmap.md) 参照)にも載せる。

## 保管庫の一括 HTML エクスポート(出自: 2026-07-13 検討・前提: 上記 `link:*.adoc` 書き換え)

- 保管庫内の全 .adoc を、フォルダ構造をミラーしつつ出力先ディレクトリへ
  HTML エクスポートする。各ファイルは既存の `buildExportHtml` をそのまま通す
  (readDir 走査済みのツリー → readTextFile → buildExportHtml → writeTextFile のループ)。
- **前提タスク**: `link:*.adoc` → `.html` 書き換え(上記)。一括エクスポートでは
  文書間リンクが機能の本体になるため先行させる(`xref:` は既に `.html` になる)。
- PDF の一括は対象外(`window.print()` 方式では不可能。必要になったら印刷方式ごと見直す)。
- 設計判断:
  - **mkdir 権限**: 出力先にサブフォルダを掘るには fs プラグインの mkdir 系許可を
    capabilities に追加する必要がある(現状 read/write のみ)。識別子はスキーマで
    確認し、verify-tauri チェックリストにも追記する。
  - **アセットのコピー**: `image::foo.png[]` 等の相対参照を生かすには画像も
    コピーが要る。「.adoc 以外も丸ごとコピー」が単純で堅いが、大きいファイルの
    無駄をどうするか。
  - **進捗・エラー UI**: Mermaid 焼き込みは文書ごと、Kroki 有効時は図ごとに
    外部通信が入るため、文書数が多いと数十秒かかりうる。進捗表示と
    失敗ファイルのサマリーが実質必須。
  - **上書きの扱い**: 既存出力先への再エクスポート時に確認を挟むか。
- 規模感: 前提タスク込みで半日〜1日のマイルストーン級。

## `include::` ディレクティブ対応(出自: 2026-07-13 検討・1マイルストーン級)

- 動かない直接の原因は `safe: "safe"` ではなく、ブラウザ環境の Asciidoctor.js が
  ファイルを読めないこと(SAFE モードはカスタム include 処理を許す。
  完全禁止は SECURE のみ)。
- 実装方針(確立されたパターン): 変換前に `include::` 対象パスを再帰走査して
  Tauri fs で非同期プリロード → キャッシュから同期的に返す `IncludeProcessor`
  拡張を登録して変換。走査は `core/` に純関数+テストで置く。
  循環参照の検出・再帰深さ上限も必要。
- **設計判断① セキュリティ境界**: fs スコープが `**` のため、無制限だと
  第三者の .adoc が任意のローカルファイルを include してプレビュー/エクスポートに
  混入させられる。「保管庫ルート(または文書のディレクトリ)配下のみ許可」の
  境界を設計時に確定させること。
- **設計判断② スクロール同期**: 現行の同期はエディタ見出し行とプレビュー h1〜h6 を
  出現順で1対1対応させるため、include 由来の見出しがプレビュー側にだけ増えると
  対応がズレる。include 由来の見出しをアンカー対象から除外する仕掛けが要る。
- 付随論点: 未保存文書は基準ディレクトリがなく相対 include を解決できない
  (エラー表示の設計)、`tags=` / `lines=` のサポート範囲、再変換時の読み直し
  キャッシュ、include 先の外部変更の反映(手動リフレッシュに乗せる)、
  HTML エクスポートへの同経路適用。

## アウトラインパネル(出自: 2026-07-13 `:toc: left` の検討)

- `:toc: left` の左固定表示は Asciidoctor 公式スタイルシートの機能で、このアプリでは
  効かない(実測確認済み): プレビューの埋め込み出力では位置指定が HTML に
  残らず(素の `:toc:` と同一出力)、standalone エクスポートでは `body.toc2.toc-left`
  クラスは付くが自前 CSS に `.toc2` のレイアウトがない。
- プレビューペイン内に左固定 TOC を作り込むのではなく、**Obsidian のような独立した
  「アウトライン」パネル**(エディタ/プレビューとは別に出せる見出しツリー)として
  設計する方がアプリの方向性(Obsidian ライク)に合う。分割ビューでは
  プレビューの幅が狭く、固定サイドバーを差し込む余地が小さいという事情もある。
- 土台: `core/headings.ts` の見出し抽出が既にある。クリック時のジャンプは
  スクロール同期の `headingAnchors`(エディタ行 ⇔ プレビュー見出しの対応)を
  流用できる見込み。
- エクスポート HTML での `:toc: left` 対応(`.toc2` 用 CSS の追加)は本件とは独立の
  小タスク。需要が出たら別途判断する。

## README の英語化(英語 README.md + 日本語 README.ja.md)

- 現在の日本語 README を `README.ja.md` へ移し、英語版 `README.md` を新規に書く
  (GitHub に閲覧者の言語で自動切り替えする機能はないため、この2ファイル構成が定番)。
- 両ファイルの冒頭に相互リンクを付ける: `[English](README.md) | [日本語](README.ja.md)`
- M19 のリブランディング(名称反映で README も触る)と同時期にやると二度手間がない。

## ドロップダウン開閉配線の共通化(出自: M17 #3・3例目が現れたら)

- `core/menu.ts` の状態機械の実質的な価値は「複数メニュー間のホバー切り替え」。
  タブ一覧は単独ドロップダウンなので、共有できる core ロジックは boolean 1個分しかなく、
  状態機械の再利用は形だけの統一になる。
- 実際の重複は ui 層の「外側クリック + Escape で閉じる」配線だが、両者は方式が異なる
  (menubar: 常設の window `click` リスナー + 各所の `stopPropagation` 前提 /
  tabs: 開いている間だけの `pointerdown` capture)。統一するには menubar 側の
  stopPropagation 連鎖を触ることになり、ユーザー可視の利得ゼロに対して回帰リスクが見合わない。
- 将来ドロップダウンが 3 例目として増えた時点で、共通ヘルパー(`ui/dropdown.ts` 案)を
  抽出するのが妥当。
